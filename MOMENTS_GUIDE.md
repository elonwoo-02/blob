# 朋友圈使用指南

## 📝 如何添加新动态

### 1. 编辑数据文件
打开文件 `src/data/moments.ts`

### 2. 添加新动态
在 `moments` 数组的**最前面**添加新对象（最新的动态显示在最上面）：

```typescript
{
  id: '7',  // 唯一ID，按顺序递增
  name: 'Elon Woo',  // 你的昵称
  avatar: 'EW',  // 1-2个字的缩写
  avatarBg: '#fef3c7',  // 头像背景色
  avatarColor: '#92400e',  // 头像文字颜色
  time: '刚刚',  // 发布时间
  text: '这是一条新的朋友圈动态！',
  images: ['/moments/new-photo.jpg'],  // 图片路径（相对于public目录）
  location: '上海',  // 位置（可选）
  likes: ['林夏', '阿泽'],  // 点赞列表
  comments: [
    { name: '林夏', text: '好棒！' },
  ],
}
```

### 3. 添加图片
- 将图片放到 `public/moments/` 目录
- 推荐命名：`photo-1.jpg`, `photo-2.png` 等
- 图片路径写为：`'/moments/photo-1.jpg'`

### 4. 提交到Git
```bash
git add .
git commit -m "添加新的朋友圈动态"
git push
```

## 🎨 头像配色推荐

```typescript
// 绿色系
avatarBg: '#d1fae5', avatarColor: '#065f46'

// 蓝色系
avatarBg: '#dbeafe', avatarColor: '#1e40af'

// 黄色系
avatarBg: '#fef3c7', avatarColor: '#92400e'

// 粉色系
avatarBg: '#fce7f3', avatarColor: '#9d174d'

// 紫色系
avatarBg: '#e0e7ff', avatarColor: '#3730a3'
```

## 📸 图片布局说明

朋友圈会根据图片数量自动调整布局：
- **1张图**：大图显示（最大200px宽）
- **2张图**：横向2列
- **3张图**：横向3列
- **4张图**：2×2网格
- **5-6张图**：3列网格
- **7-9张图**：3×3网格

## ⏰ 时间格式建议

```typescript
time: '刚刚'
time: '5分钟前'
time: '2小时前'
time: '昨天 23:16'
time: '前天 18:42'
time: '3天前'
time: '2024-12-25'
```

## 💬 评论格式

### 普通评论
```typescript
{ name: '林夏', text: '很棒！' }
```

### 回复评论
```typescript
{ name: 'Elon Woo', text: '谢谢！', replyTo: '林夏' }
```
显示为：**Elon Woo** 回复 **林夏**：谢谢！

## 🗂️ 文件结构

```
dimensional-debris/
├── src/
│   └── data/
│       └── moments.ts          # 朋友圈数据文件（主要编辑这个）
├── public/
│   └── moments/                # 朋友圈图片目录
│       ├── sunset-1.jpg
│       ├── sunset-2.jpg
│       └── ...
└── src/pages/
    └── moment.astro            # 朋友圈页面（不需要修改）
```

## 🚀 工作流程示例

### 发布一条带3张照片的动态

1. **准备图片**
   - 重命名为：`trip-1.jpg`, `trip-2.jpg`, `trip-3.jpg`
   - 移动到：`public/moments/`

2. **编辑 `src/data/moments.ts`**
   ```typescript
   export const moments: Moment[] = [
     // 👇 在数组最前面添加
     {
       id: '7',
       name: 'Elon Woo',
       avatar: 'EW',
       avatarBg: '#fef3c7',
       avatarColor: '#92400e',
       time: '刚刚',
       text: '周末去爬山，景色超美！推荐大家来玩 🏔️',
       images: [
         '/moments/trip-1.jpg',
         '/moments/trip-2.jpg',
         '/moments/trip-3.jpg'
       ],
       location: '黄山',
       likes: [],
       comments: [],
     },
     // 之前的动态...
   ]
   ```

3. **提交**
   ```bash
   git add .
   git commit -m "添加黄山游记"
   git push
   ```

4. **完成！** 
   访问 `http://localhost:4321/moment` 或部署后的URL查看

## 📌 注意事项

- ID必须唯一，建议递增
- 图片路径开头要加 `/`
- 图片格式支持：`.jpg`, `.png`, `.webp`, `.gif`
- 最多上传9张图片
- location可以留空字符串（不显示位置）
- likes和comments可以是空数组
