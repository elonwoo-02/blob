---
// HeatmapSection.astro - Blog update heatmap visualization
---

<div class="heatmap-section p-4">
  <h3 class="text-sm font-semibold mb-3 text-base-content/70">博客更新热力图</h3>
  <div id="heatmap-container" class="overflow-x-auto">
    <!-- Heatmap will be rendered here -->
    <div class="grid grid-cols-7 gap-1">
      {Array.from({ length: 56 }).map((_, i) => {
        const intensity = Math.floor(Math.random() * 5);
        const colors = [
          'bg-base-300',
          'bg-primary/20',
          'bg-primary/40',
          'bg-primary/60',
          'bg-primary/80',
        ];
        return (
          <div
            class={`w-3 h-3 rounded-sm ${colors[intensity]} hover:ring-2 hover:ring-primary transition-all cursor-pointer`}
            title={`Day ${i + 1}`}
          />
        );
      })}
    </div>
  </div>
</div>

<style>
  .heatmap-section {
    min-height: 120px;
  }
</style>
