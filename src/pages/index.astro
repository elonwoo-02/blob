---
import Greeting from '../components/Greeting';
import BaseLayout from '../layouts/BaseLayout.astro';
const pageTitle = "";
---

<BaseLayout pageTitle={pageTitle}>
  <section class="cli-shell" aria-label="command line page">
    <div class="cli-line">
      <span class="prompt">dev@terminal</span>
      <span class="path">~/workspace/astro</span>
      <span class="symbol">$</span>
      <span class="command">cat README.md</span>
    </div>
    <div class="cli-block">
      <h1 class="cli-title">程序员的命令行日志</h1>
      <p class="cli-text">记录构建、调试与复盘，用终端视角梳理每一次迭代。</p>
    </div>
    <div class="cli-line">
      <span class="prompt">dev@terminal</span>
      <span class="path">~/workspace/astro</span>
      <span class="symbol">$</span>
      <span class="command">ls</span>
    </div>
    <div class="cli-block">
      <a class="cli-link" href="/blog/">blog/</a>
      <a class="cli-link" href="/tags/">tags/</a>
      <a class="cli-link" href="/posts/">posts/</a>
      <a class="cli-link" href="/about/">about/</a>
    </div>
    <div class="cli-line">
      <span class="prompt">dev@terminal</span>
      <span class="path">~/workspace/astro</span>
      <span class="symbol">$</span>
      <span class="command">tail -n 3 status.log</span>
    </div>
    <div class="cli-block">
      <p class="cli-text">[ok] build: green</p>
      <p class="cli-text">[ok] tests: passing</p>
      <p class="cli-text">[ok] deploy: ready</p>
    </div>
    <div class="cli-line">
      <span class="prompt">dev@terminal</span>
      <span class="path">~/workspace/astro</span>
      <span class="symbol">$</span>
      <span class="command">./greet</span>
    </div>
    <div class="cli-block">
      <Greeting client:load messages={["Hi", "Hello", "Howdy", "Hey there"]} />
      <p class="cursor">█</p>
    </div>
  </section>

  <style is:inline>
    .cli-shell {
      font-family: "JetBrains Mono", "Fira Code", "SFMono-Regular", "Consolas", "Liberation Mono", "Menlo", monospace;
      background: transparent;
      border: none;
      border-radius: 0;
      padding: 1rem 0;
      box-shadow: none;
    }

    .cli-line {
      display: flex;
      flex-wrap: wrap;
      gap: 0.6rem;
      align-items: baseline;
      margin: 0 0 0.6rem;
      font-size: 0.95rem;
    }

    .prompt {
      color: #111827;
      font-weight: 600;
    }

    .path {
      color: #1f2937;
    }

    .symbol {
      color: #111827;
    }

    .command {
      color: #111827;
    }

    .cli-block {
      padding: 0 0 1.4rem 1.2rem;
      color: #111827;
    }

    .cli-title {
      font-size: 1.9rem;
      margin: 0 0 0.5rem;
      color: #111827;
    }

    .cli-text {
      margin: 0 0 0.3rem;
      font-size: 0.95rem;
    }

    .cli-link {
      margin-right: 1rem;
      color: #111827;
      text-decoration: underline;
      text-underline-offset: 3px;
    }

    .cli-link:hover {
      text-decoration: underline;
    }

    .cursor {
      margin: 0.2rem 0 0;
      animation: blink 1s steps(2, start) infinite;
      color: #111827;
    }

    @keyframes blink {
      0%,
      100% {
        opacity: 0;
      }
      50% {
        opacity: 1;
      }
    }

    @media (max-width: 640px) {
      .cli-shell {
        padding: 0.75rem 0;
      }

      .cli-line {
        font-size: 0.85rem;
      }

      .cli-title {
        font-size: 1.6rem;
      }
    }

    :global(header) {
      display: none;
    }

    :global(.hidden.md\:block.relative) {
      display: none !important;
    }

    :global(main) {
      padding-top: 1.5rem !important;
    }
  </style>
</BaseLayout>
