---
import BaseLayout from '../layouts/BaseLayout.astro';

const pageTitle = "指令中心";

const quickLinks = [
  {
    title: "终端日志",
    description: "最新推送、排障记录与安全公告。",
    command: "tail -f /var/log/mission.log",
  },
  {
    title: "漏洞追踪",
    description: "实时监测关键依赖的风险与补丁进度。",
    command: "npx audit --live",
  },
  {
    title: "知识档案",
    description: "系统化整理的研究笔记与战术复盘。",
    command: "cat ~/vault/notes.md",
  },
];

const statusCards = [
  {
    label: "节点在线",
    value: "24/24",
    hint: "心跳稳定",
  },
  {
    label: "运行时延",
    value: "18ms",
    hint: "低延迟链路",
  },
  {
    label: "加密等级",
    value: "AES-256",
    hint: "全域加固",
  },
];

const missions = [
  {
    title: "夜间监控",
    detail: "追踪异常流量，生成自动化告警。",
    status: "进行中",
  },
  {
    title: "防御演练",
    detail: "模拟攻击路径，优化响应手册。",
    status: "待启动",
  },
  {
    title: "数据迁移",
    detail: "将核心备份同步到冷存储节点。",
    status: "已完成",
  },
];
---

<BaseLayout pageTitle={pageTitle}>
  <section class="relative overflow-hidden rounded-3xl border border-emerald-500/30 bg-black/80 p-8 text-slate-100 shadow-[0_0_40px_rgba(16,185,129,0.15)]">
    <div class="pointer-events-none absolute inset-0 bg-[radial-gradient(circle_at_top,#10b9811a,transparent_60%)]"></div>
    <div class="relative grid gap-10 lg:grid-cols-[1.2fr_0.8fr]">
      <div>
        <div class="mb-4 inline-flex items-center gap-2 rounded-full border border-emerald-500/40 bg-emerald-500/10 px-4 py-1 text-xs font-mono uppercase tracking-[0.25em] text-emerald-300">
          <span class="h-2 w-2 rounded-full bg-emerald-400 shadow-[0_0_10px_rgba(16,185,129,0.8)]"></span>
          系统已上线
        </div>
        <h2 class="text-3xl font-bold text-emerald-200 md:text-4xl">欢迎进入黑客风格指挥台</h2>
        <p class="mt-4 max-w-xl text-sm leading-relaxed text-slate-300 md:text-base">
          这里是你的主控终端：集中展示数据流、任务进度与关键指令。将内容组织成高对比度的终端面板，让每一次操作都像在执行深夜渗透任务。
        </p>
        <div class="mt-8 grid gap-4 sm:grid-cols-3">
          {statusCards.map((card) => (
            <div class="rounded-2xl border border-emerald-500/30 bg-black/60 p-4">
              <p class="text-xs font-mono uppercase tracking-widest text-emerald-400">
                {card.label}
              </p>
              <p class="mt-2 text-2xl font-semibold text-emerald-100">{card.value}</p>
              <p class="mt-1 text-xs text-slate-400">{card.hint}</p>
            </div>
          ))}
        </div>
      </div>
      <div class="rounded-2xl border border-emerald-500/30 bg-black/70 p-6 font-mono text-sm text-emerald-200 shadow-[inset_0_0_20px_rgba(16,185,129,0.2)]">
        <div class="flex items-center gap-2 text-xs text-emerald-400">
          <span class="h-2 w-2 rounded-full bg-emerald-400"></span>
          terminal@blob:~$ status
        </div>
        <pre class="mt-4 whitespace-pre-wrap leading-relaxed text-emerald-200">
[init] 连接中...
[ok]   核心服务已激活
[ok]   防火墙规则已加载
[ok]   日志流已同步
[info] 下一次扫描: 04:00
        </pre>
        <div class="mt-4 border-t border-emerald-500/20 pt-4 text-xs text-emerald-400">
          输入 <span class="text-emerald-200">help</span> 获取更多指令
        </div>
      </div>
    </div>
  </section>

  <section class="mt-12 grid gap-6 lg:grid-cols-[0.8fr_1.2fr]">
    <div class="rounded-3xl border border-emerald-500/30 bg-black/70 p-6">
      <h3 class="text-lg font-semibold text-emerald-200">任务清单</h3>
      <p class="mt-2 text-sm text-slate-400">保持行动节奏，确保每个模块都在掌控之中。</p>
      <div class="mt-6 space-y-4">
        {missions.map((mission) => (
          <div class="rounded-2xl border border-emerald-500/20 bg-black/50 p-4">
            <div class="flex items-center justify-between text-sm text-emerald-300">
              <span class="font-semibold">{mission.title}</span>
              <span class="rounded-full border border-emerald-500/30 px-2 py-0.5 text-xs text-emerald-200">
                {mission.status}
              </span>
            </div>
            <p class="mt-2 text-xs text-slate-400">{mission.detail}</p>
          </div>
        ))}
      </div>
    </div>

    <div class="rounded-3xl border border-emerald-500/30 bg-black/70 p-6">
      <h3 class="text-lg font-semibold text-emerald-200">快速入口</h3>
      <p class="mt-2 text-sm text-slate-400">一键到达关键模块，或复制终端命令继续深入。</p>
      <div class="mt-6 grid gap-4">
        {quickLinks.map((link) => (
          <div class="rounded-2xl border border-emerald-500/20 bg-black/50 p-4">
            <div class="flex items-center justify-between">
              <div>
                <p class="text-sm font-semibold text-emerald-200">{link.title}</p>
                <p class="mt-1 text-xs text-slate-400">{link.description}</p>
              </div>
              <span class="rounded-full border border-emerald-500/30 px-3 py-1 text-xs text-emerald-300">
                open
              </span>
            </div>
            <div class="mt-3 rounded-xl border border-emerald-500/20 bg-black/80 px-3 py-2 text-xs font-mono text-emerald-300">
              {link.command}
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <section class="mt-12 rounded-3xl border border-emerald-500/30 bg-black/70 p-6">
    <h3 class="text-lg font-semibold text-emerald-200">指挥台提示</h3>
    <div class="mt-4 grid gap-4 md:grid-cols-3">
      <div class="rounded-2xl border border-emerald-500/20 bg-black/50 p-4">
        <p class="text-xs font-mono uppercase tracking-widest text-emerald-400">安全扫描</p>
        <p class="mt-2 text-sm text-slate-300">每 6 小时刷新漏洞库，保持防线高亮。</p>
      </div>
      <div class="rounded-2xl border border-emerald-500/20 bg-black/50 p-4">
        <p class="text-xs font-mono uppercase tracking-widest text-emerald-400">部署节奏</p>
        <p class="mt-2 text-sm text-slate-300">保持灰度发布，避免波峰流量突增。</p>
      </div>
      <div class="rounded-2xl border border-emerald-500/20 bg-black/50 p-4">
        <p class="text-xs font-mono uppercase tracking-widest text-emerald-400">日志洞察</p>
        <p class="mt-2 text-sm text-slate-300">标记关键异常，自动生成可视化报告。</p>
      </div>
    </div>
  </section>
</BaseLayout>
