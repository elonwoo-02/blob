---
import BaseLayout from '../layouts/BaseLayout.astro';

const cards = [
  {
    time: '2026-01-19 20:33:59',
    tag: '#Diary/Records',
    content: '胖老姨收了几万块贿赂，官方进行了诱供。',
  },
  {
    time: '2026-01-11 02:22:27',
    tag: '#Diary/Dreams',
    content: '梦到算法考试后，和朋友们出去玩，各位朋友好久没见，后来变成恐怖片',
  },
  {
    time: '2026-01-10 06:02:56',
    tag: '#Diary/Dreams',
    content: '梦到制作的人工智能，藏在松青小屋的柜子里，那是她抱着她数据安全和格式化的地方，外表是我的妹妹',
  },
  {
    time: '2026-01-09 10:02:04',
    tag: '#Diary/Thought',
    content:
      '思考：在近代帝国主义进行商品倾销的情况下，抵制外货具有重要作用；但在当下全球化和国际分工的历史背景下，全球贸易使得每一个参与者互利共赢，抵制外货的作用减少。然而，当经济体量足够大，且有合适的替代品，仍然可以通过抵制外货进行经济的反制。',
  },
  {
    time: '2026-01-09 09:46:18',
    tag: '#Diary/Notes',
    content: '党史学习',
  },
];

const heatmapRows = 7;
const heatmapCols = 12;
const activeCells = [2, 9, 14, 18, 23, 29, 34, 36, 41, 48, 54, 58, 63, 69, 72, 77];
---

<BaseLayout title="Test - Flomo UI Mock">
  <section class="flomo-page">
    <div id="flomo-app" class="flomo-app" data-open="false">
      <aside class="sidebar" id="sidebar">
        <div class="sidebar-top-icons">
          <button type="button" class="icon-btn" aria-label="通知">◠</button>
          <button type="button" class="icon-btn" aria-label="设置">⬡</button>
        </div>

        <div class="profile-row">
          <h2>温雅小公子</h2>
          <span>⚡Upgrade PRO</span>
        </div>

        <div class="stat-grid">
          <div><strong>1602</strong><span>MEMO</span></div>
          <div><strong>60</strong><span>TAG</span></div>
          <div><strong>1543</strong><span>DAY</span></div>
        </div>

        <div class="heatmap" aria-hidden="true">
          {Array.from({ length: heatmapRows }).map((_, row) => (
            <div class="heatmap-row">
              {Array.from({ length: heatmapCols }).map((__, col) => {
                const index = row * heatmapCols + col;
                const isActive = activeCells.includes(index);
                return <span class:list={{ cell: true, active: isActive }} />;
              })}
            </div>
          ))}
          <div class="months"><span>Dec</span><span>Jan</span><span>Feb</span></div>
        </div>

        <nav class="side-nav">
          <a href="#">MEMO</a>
          <a href="#">WeChat Input</a>
          <a href="#">Daily Review</a>
          <a href="#">AI Insights</a>
        </nav>

        <div class="tag-title">Pinned Tags</div>
        <button class="pinned-tag" type="button"># Diary</button>

        <footer class="side-footer">
          <a href="#">Trash</a>
          <a href="#">Helper Center</a>
          <span>Since 2020.04</span>
        </footer>
      </aside>

      <main class="timeline">
        <header class="topbar">
          <button id="sidebar-toggle" class="top-icon" type="button" aria-label="打开侧边栏">☰</button>
          <h1>#Diary</h1>
          <button class="top-icon" type="button" aria-label="搜索">⌕</button>
        </header>

        <div class="cards">
          {cards.map((card) => (
            <article class="memo-card">
              <div class="card-meta">
                <time>{card.time}</time>
                <button type="button" aria-label="更多">•••</button>
              </div>
              <span class="card-tag">{card.tag}</span>
              <p>{card.content}</p>
            </article>
          ))}
        </div>

        <button class="floating-filter" type="button" aria-label="过滤">
          <span></span><span></span><span></span>
        </button>
        <button class="floating-add" type="button" aria-label="新增">+</button>
      </main>
    </div>
  </section>

  <script>
    const app = document.getElementById('flomo-app');
    const toggle = document.getElementById('sidebar-toggle');

    if (app && toggle) {
      toggle.addEventListener('click', () => {
        const next = app.dataset.open !== 'true';
        app.dataset.open = next ? 'true' : 'false';
      });
    }
  </script>
</BaseLayout>

<style>
  .flomo-page {
    min-height: calc(100vh - 5rem);
    padding: 0.75rem;
    background: #e8e8e8;
  }

  .flomo-app {
    position: relative;
    display: flex;
    min-height: calc(100vh - 6.5rem);
    border-radius: 20px;
    overflow: hidden;
    background: #ececec;
    color: #4b4b4b;
  }

  .sidebar {
    width: 0;
    padding: 1.25rem 0;
    background: #ececec;
    overflow: hidden;
    transition: width 0.28s ease, padding 0.28s ease;
  }

  .flomo-app[data-open='true'] .sidebar {
    width: min(340px, 78vw);
    padding: 1.25rem 1.5rem;
  }

  .sidebar-top-icons {
    display: flex;
    gap: 0.75rem;
    margin-bottom: 1rem;
  }

  .icon-btn,
  .top-icon {
    border: none;
    background: transparent;
    color: #9c9c9c;
    font-size: 1.35rem;
    cursor: pointer;
    line-height: 1;
  }

  .profile-row {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-bottom: 1rem;
  }

  .profile-row h2 {
    margin: 0;
    font-size: 2rem;
    font-weight: 700;
    color: #444;
  }

  .profile-row span {
    background: #f0e5d9;
    color: #b78f61;
    border-radius: 8px;
    padding: 0.2rem 0.45rem;
    font-size: 0.85rem;
    white-space: nowrap;
  }

  .stat-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    margin-bottom: 1rem;
  }

  .stat-grid strong {
    display: block;
    font-size: 2.7rem;
    line-height: 1;
    color: #8f8f8f;
  }

  .stat-grid span {
    font-size: 1rem;
    color: #a9a9a9;
  }

  .heatmap {
    margin-bottom: 1.5rem;
  }

  .heatmap-row {
    display: grid;
    grid-template-columns: repeat(12, 1fr);
    gap: 8px;
    margin-bottom: 8px;
  }

  .cell {
    aspect-ratio: 1;
    border-radius: 6px;
    background: #d7d7d7;
  }

  .cell.active {
    background: #b8dbcb;
  }

  .months {
    display: flex;
    justify-content: space-between;
    margin-top: 0.5rem;
    font-size: 1.25rem;
    color: #a4a4a4;
  }

  .side-nav,
  .side-footer {
    display: flex;
    flex-direction: column;
    gap: 0.95rem;
  }

  .side-nav a,
  .side-footer a {
    text-decoration: none;
    color: #4f4f4f;
    font-size: 1.9rem;
    font-weight: 500;
  }

  .tag-title {
    margin: 1.25rem 0 0.7rem;
    color: #b3b3b3;
    font-size: 1.4rem;
  }

  .pinned-tag {
    border: none;
    border-radius: 16px;
    background: #33cd79;
    color: #fff;
    width: 100%;
    text-align: left;
    font-size: 2rem;
    padding: 0.7rem 1rem;
    margin-bottom: 1.2rem;
  }

  .side-footer {
    margin-top: auto;
  }

  .side-footer span {
    margin-top: 1rem;
    color: #b4b4b4;
    font-size: 1.5rem;
  }

  .timeline {
    flex: 1;
    min-width: 0;
    padding: 0.5rem 1rem 1.25rem;
  }

  .topbar {
    display: grid;
    grid-template-columns: 44px 1fr 44px;
    align-items: center;
    color: #3f3f3f;
    margin-bottom: 0.6rem;
  }

  .topbar h1 {
    margin: 0;
    text-align: center;
    font-size: clamp(1.6rem, 2vw, 2.2rem);
    font-weight: 700;
  }

  .cards {
    display: flex;
    flex-direction: column;
    gap: 0.95rem;
  }

  .memo-card {
    background: #f2f2f2;
    border-radius: 16px;
    padding: 1.05rem 1.3rem;
  }

  .card-meta {
    display: flex;
    justify-content: space-between;
    align-items: center;
    color: #9c9c9c;
    margin-bottom: 0.6rem;
  }

  .card-meta time {
    font-size: clamp(0.85rem, 1.2vw, 1rem);
  }

  .card-meta button {
    border: none;
    background: transparent;
    color: #9c9c9c;
  }

  .card-tag {
    display: inline-flex;
    align-items: center;
    border-radius: 8px;
    background: #dce6f7;
    color: #6f92cd;
    padding: 0.18rem 0.5rem;
    margin-bottom: 0.55rem;
    font-size: clamp(0.9rem, 1.2vw, 1.1rem);
  }

  .memo-card p {
    margin: 0;
    color: #444;
    line-height: 1.56;
    font-size: clamp(1.05rem, 1.45vw, 1.6rem);
  }

  .floating-filter,
  .floating-add {
    position: fixed;
    right: 1.2rem;
    width: 54px;
    height: 54px;
    border: none;
    border-radius: 14px;
    box-shadow: 0 6px 16px rgba(0, 0, 0, 0.1);
    cursor: pointer;
  }

  .floating-filter {
    top: 50%;
    transform: translateY(-50%);
    background: #eff9f3;
    display: inline-flex;
    flex-direction: column;
    justify-content: center;
    gap: 5px;
    padding: 0 14px;
  }

  .floating-filter span {
    width: 24px;
    height: 3px;
    border-radius: 99px;
    background: #29c271;
  }

  .floating-add {
    bottom: 1.5rem;
    background: #2eca73;
    color: #fff;
    font-size: 2.3rem;
  }

  @media (max-width: 960px) {
    .flomo-page {
      padding: 0;
    }

    .flomo-app {
      border-radius: 0;
      min-height: calc(100vh - 4.5rem);
    }

    .flomo-app[data-open='true'] .sidebar {
      width: min(320px, 88vw);
    }

    .side-nav a,
    .side-footer a {
      font-size: 1.35rem;
    }

    .profile-row h2 {
      font-size: 1.6rem;
    }

    .stat-grid strong {
      font-size: 2rem;
    }
  }
</style>
